def solution(n):
    # 도착점 n에서 거꾸로 되돌아간다: 짝수면 순간이동을 되돌려 반으로 줄이고(건전지 소모 없음), 홀수면 1칸 점프를 되돌린다(건전지 1 소모). 되돌린 점프 횟수가 곧 최소 건전지 사용량이다.
    # 시간 복잡도: O(log n)
    cnt = 0
    while n > 0:
        if n % 2:
            n -= 1
            cnt += 1
        else:
            n //= 2
    return cnt
